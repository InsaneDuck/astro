import { PostView } from "lemmy-js-client";
import { FC } from "react";
import { StyleSheet } from "react-native";

type PostViewFeedProps = {
  postView: PostView;
};
export const PostViewFeed: FC<PostViewFeedProps> = (props) => {
  return <></>;
};

const styles = StyleSheet.create({});
