import { FC } from "react";
import { StyleSheet } from "react-native";

import { Text } from "@/common/Text";

type PostTitleProps = object;
export const PostTitle: FC<PostTitleProps> = (props) => {
  return <Text />;
};

const styles = StyleSheet.create({});
