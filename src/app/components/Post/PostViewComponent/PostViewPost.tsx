import { PostView } from "lemmy-js-client";
import { FC } from "react";
import { StyleSheet } from "react-native";

type PostViewPostProps = {
  postView: PostView;
};
export const PostViewPost: FC<PostViewPostProps> = (props) => {
  return <></>;
};

const styles = StyleSheet.create({});
