import { FC } from "react";
import { StyleSheet } from "react-native";

type TrendingProps = object;
export const Trending: FC<TrendingProps> = (props) => {
  return <></>;
};

const styles = StyleSheet.create({});
