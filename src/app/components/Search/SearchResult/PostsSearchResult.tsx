import { FC } from "react";
import { StyleSheet } from "react-native";

type PostsSearchResultProps = object;
export const PostsSearchResult: FC<PostsSearchResultProps> = (props) => {
  return <></>;
};

const styles = StyleSheet.create({});
