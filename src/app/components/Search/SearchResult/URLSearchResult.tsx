import { FC } from "react";
import { StyleSheet } from "react-native";

type UrlSearchResultProps = object;
export const URLSearchResult: FC<UrlSearchResultProps> = (props) => {
  return <></>;
};

const styles = StyleSheet.create({});
